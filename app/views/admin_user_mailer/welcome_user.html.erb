<!-- app/views/admin_user_mailer/welcome_user.html.erb -->
<!DOCTYPE html>
<html>
  <body>
    <h1>Welcome, <%= @user.first_name %>!</h1>
    <p>
      We’ve created your account in the ISA Dental system.
      <br/>
      <% if @temp_password.present? %>
        Your temporary password is: <strong><%= @temp_password %></strong>
      <% else %>
        You already have your password set.
      <% end %>
    </p>
    <p>Please log in and check your upcoming appointments.</p>
  </body>
</html>

<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Appointment Reminder</title>
  <style>
    /* Basic email styles here, similar to your existing ones */
  </style>
</head>
<body>
  <h1>Appointment Reminder</h1>
  <p>
    Hi <%= @user.first_name %>,
  </p>
  <p>
    This is a reminder for your dental appointment on 
    <strong><%= @appointment.appointment_time.in_time_zone.strftime('%B %d, %Y at %I:%M %p') %></strong>.
  </p>
  <% if @appointment.dependent_id? %>
    <p>
      Reminder for your dependent (<%= @appointment.dependent&.first_name %> <%= @appointment.dependent&.last_name %>).
    </p>
  <% end %>
  <p>
    <a href="<%= @appointment_url %>">
      View Details
    </a>
  </p>
  <p>
    See you soon!
  </p>
</body>
</html>
